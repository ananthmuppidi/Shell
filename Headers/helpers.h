#ifndef __HELPERS__H_
#define __HELPERS__H_

/// @breif Counts the number of digits (for padding purposes)
/// @param n The number whos digits need to be counted
int countDigits(int n);

/// @breif Returns the bigger number
/// @param a First number
/// @param b Second number
int max(int a, int b);

#endif
